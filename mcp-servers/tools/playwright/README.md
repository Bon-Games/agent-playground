# Playwright MCP

Docker Hub image: `mcp/playwright:latest`

Provides browser automation tools (navigate, click, screenshot, fill forms) to Claude via MCP.

## Setup

No environment variables required. The container runs a headless Chromium browser.

## Source

https://hub.docker.com/r/mcp/playwright
